version: 0.0.1
server:
  port: 8080
---
spring:
  config:
    activate:
      on-profile: default,development
  redis:
    host: localhost
    port: 6379
    password: "ta,TvT5P]ox<Ho0vs"
    database: 0
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
hexa-page-jwt-secret-key: HexaH0mePageBackend
secretIv: 1
